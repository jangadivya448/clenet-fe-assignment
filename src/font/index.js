import { Lexend,  Saira } from "next/font/google";

const lexed = Lexend({
  weight: ["400", "600", "700"],
  subsets: ["latin"],
  display: "swap",
});

const comfortaa = Saira({
  weight: ["400", "600", "700"],
  subsets: ["latin"],
  display: "swap",
});

export { lexed, comfortaa };
